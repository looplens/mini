import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 0,
    backgroundColor: "#F33AD1",
    position: "absolute",
    height: 60,
    width: "100%",
    bottom: 0,
  },
});

export default styles;
